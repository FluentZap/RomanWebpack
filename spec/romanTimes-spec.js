import { convertToRoman } from '../src/romanTimes';

describe('convertToRoman', function () {

  it("It's a test Test", function () {
    //Test content will go here.
    expect(convertToRoman("1")).toEqual("I");
    expect(convertToRoman("10")).toEqual("X");
    expect(convertToRoman("50")).toEqual("L");
    expect(convertToRoman("100")).toEqual("C");
    expect(convertToRoman("1000"))
  });
});

describe('converToRoman', function () {

  it("Does not equal")
})